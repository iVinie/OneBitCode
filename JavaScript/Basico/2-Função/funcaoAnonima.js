let doubleSpeed = function(velocity){
    return velocity*2
}

let velocity = (doubleSpeed(30))

alert (velocity)

//A função so funciona chamando a variável e se eu chamar a função antes de declarar a mesma, ela não funiona, como a tradicional